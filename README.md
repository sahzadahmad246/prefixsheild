# prefixsheild

Android app (PrefixShield) that blocks incoming calls by number series, without replacing Google Phone.

Uses the Call screening role. Prefixes are saved on the device (SharedPreferences).
